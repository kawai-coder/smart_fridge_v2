# -----------------------------
# Streamlit UI
# -----------------------------
streamlit==1.36.0
pandas==2.2.2
numpy==1.26.4
altair==5.5.0

# -----------------------------
# HTTP + Image
# -----------------------------
requests==2.32.5
pillow==10.4.0

# -----------------------------
# Transformers for OwlViT (use image's torch 2.8.0 + cuda12.4)
# DO NOT install torch/torchvision here to avoid overwriting GPU torch
# -----------------------------
transformers>=4.44,<4.49
tokenizers>=0.19,<0.21
huggingface_hub>=0.23
safetensors>=0.4.3
accelerate>=0.30,<1.0

# -----------------------------
# Useful utilities (safe)
# -----------------------------
pyyaml>=6.0
rich>=13.7
