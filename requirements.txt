# Core web app
streamlit==1.36.0

# Data / charts (dashboard & tables)
pandas==2.2.2
numpy==1.26.4
altair==5.5.0

# HTTP + images (your code imports requests + PIL)
requests==2.32.5
pillow==10.4.0

# Quality-of-life (safe, small)
pyyaml>=6.0
rich>=13.7
